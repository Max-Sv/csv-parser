import * as CsvParser from './csvParser'

describe('csvParser', () => {
  it('Parser get a correct path of File', () => {
    // const parser1 = CsvParser.default;
    // expect(parser.filePath).toBe('C:/mifort/CSV-Parser/src/Data.csv');
  });
})
